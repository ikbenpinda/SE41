<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools" android:layout_width="match_parent"
    android:layout_height="match_parent" android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    android:paddingBottom="@dimen/activity_vertical_margin"
    tools:context="com.example.etienne.pizzapp.SettingsActivity">

    <TextView android:text="@string/lbl_settings_settings" android:layout_width="wrap_content"
        android:id="@+id/settingsTitle"
        android:layout_height="wrap_content"
        android:textSize="16pt"
        android:textColor="#000000"/>

    <TextView
        android:id="@+id/LanguageTitle"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_below="@+id/settingsTitle"
        android:textSize="14pt"
        android:layout_marginTop="20dp"
        android:text="@string/lbl_language"/>

    <RadioGroup
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_below="@+id/LanguageTitle"
        android:id="@+id/rgrp_settings">

        <RadioButton
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="Nederlands"
            android:checked="false"
            android:id="@+id/btn_lang_nederlands" />

        <RadioButton
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="English"
            android:checked="true"
            android:id="@+id/btn_lang_english" />
        </RadioGroup>

    <Button
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="@string/btn_settings_confirmsettings"
        android:id="@+id/confirmButton"
        android:layout_below="@+id/rgrp_settings"
        android:layout_alignParentStart="true" />


</RelativeLayout>
